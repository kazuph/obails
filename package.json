{
  "name": "obails",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "package:desktop": "wails3 task darwin:package",
    "install:desktop": "if [ \"$(uname -s)\" = \"Darwin\" ]; then pkill -x Obails || true; rm -rf /Applications/Obails.app && cp -R bin/obails.app /Applications/Obails.app; fi",
    "reinstall:desktop": "pnpm run package:desktop && pnpm run install:desktop"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
